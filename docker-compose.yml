version: '3.7'
services:
  smolbot:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      NODE_ENV: production
      TOKEN: '${TOKEN}'
      APPLICATION_ID: '${APPLICATION_ID}'
    volumes:
      - ./:/home/node/app
